/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package rapidexpress.util;

/**
 *
 * @author User
 */
public class Validator {
    /*Propósito: Validaciones genéricas.
Métodos estáticos:
public static boolean isValidEmail(String email):
Retorna email.matches("^[A-Za-z0-9+_.-]+@(.+)$")
public static boolean isValidPhone(String phone):
Retorna phone.matches("^[0-9]{10,15}$")
public static boolean isNotEmpty(String str):
Retorna str != null && !str.trim().isEmpty()
public static boolean isPositiveNumber(double num):
Retorna num > 0
public static boolean isValidPlaca(String placa):
Retorna placa.matches("^[A-Z]{3}-[0-9]{3}$") o formato local
public static boolean isValidPeso(double peso):
Retorna peso > 0 && peso <= 1000 (límite razonable)
public static boolean isValidAnio(int anio):
Retorna anio >= 1900 && anio <= Year.now().getValue()*/
}
